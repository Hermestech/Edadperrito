function edadPerrito(edad) {
    console.log(`Tu perrito tiene ${edad} años` )
}

function sumar(a,b) {
    var resultado = a + b;
    return resultado;
}

var miNombre = "Hermes

function nombre() {
    var miApellido = "Aguilar";
    console.log(miNombre + " " + miApellido)
}

nombre();

function edadPerro(edad) {
    var age = 16*Math.log(edad)+31;
    return parseInt(age)
}

edadPerro();